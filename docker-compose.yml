services:
  motoflow-db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: motoflow-db
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: ${SA_PASSWORD}
    ports:
      - "1433:1433"